# Telegram Anti-Spam Bot

Бот для автоматического мониторинга и удаления спам-сообщений в чатах и группах Telegram.

## 🔍 Возможности

- Автоматическое удаление сообщений, содержащих спам
- Гибкая настройка правил фильтрации через регулярные выражения
- Поддержка как приватных чатов (по ID), так и публичных (по юзернейму)
- Логирование всех действий бота
- Простота настройки и развертывания

## 🚀 Установка

1. Клонируйте репозиторий:

```bash
git clone [https://github.com/ваш-пользователь/tgspambot.git](https://github.com/ваш-пользователь/tgspambot.git)
cd tgspambot
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл .env на основе .env.example:

```bash
cp .env.example .env
```

4. Получите токен бота у @BotFather и укажите его в .env


## Настройка
Добавление чатов для мониторинга
В файле .env укажите ID чатов через запятую в переменной GROUP_IDS:

- Для приватных чатов: числовой ID
- Для публичных чатов: @username (без кавычек)

### Настройка правил фильтрации
Правила настраиваются в файле rules.py. Вы можете добавлять, удалять или изменять регулярные выражения в списке SPAM_RULES.

## Примеры правил:

- Блокировка ссылок на файлообменники
- Фильтрация сообщений с большим количеством спецсимволов
- Блокировка сообщений с криптовалютными мошенничествами
- Удаление сообщений с большим количеством заглавных букв

## 🏃 Запуск

```bash
python main.py
```

Для запуска в фоновом режиме (Linux/macOS):

```bash
nohup python main.py > bot.log 2>&1 &
```

## 🚀 Закуск через docker
Запуск:
```bash
docker-compose up --build -d
```

Остановка:
```bash
docker-compose down
```

Просмотр логов:
```bash
docker-compose logs -f
```

### 🔧 Технические детали
Python 3.8+
python-telegram-bot 20.0+
python-dotenv для загрузки переменных окружения




